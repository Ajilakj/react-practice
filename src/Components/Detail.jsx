import React from 'react'

const Detail = (props) => {
  return (
    <div>
        <p className="bottom">{props.tel}</p>
        <p className="">{props.email}</p>
    </div>
  )
}

export default Detail
